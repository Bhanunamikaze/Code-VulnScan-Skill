# Core scripts use stdlib only (ast, json, re, sqlite3, pathlib, hashlib, math)
# No required dependencies.

# Optional: improves secret scanning accuracy
# pip install pyyaml         # parse YAML manifests more accurately
# pip install tomli          # parse pyproject.toml on Python < 3.11
